{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "sandbox": {
    "_shakedown": "Measured 2026-08-22, fresh top-level session, enabled:true via settings.local.json (denyRead ~/code/civic-ai-ops/, allowWrite ~/.npm) — this is the re-measurement the prior note called for. Three probes: (a) `touch ~/.sandbox-probe-$$` -> `Operation not permitted` (sandbox initialized, did not silently pass); (b) `curl -sS -m 10 https://example.com` -> `200` (documented prompt-mode default; strictAllowlist would have denied it — untested here); (c) `ls ~/code/civic-ai-ops` -> `Operation not permitted`, same OS-syscall-level refusal as (a) — the command executed and the kernel sandbox (Seatbelt, driven by settings.local.json denyRead) denied it; not a Claude Code tool-permission prompt, which would have blocked before exec with different wording. `eval \"$(fnm env)\" && fnm use 22` failed — verbatim: Can't create the symlink for multishells at \"/Users/npstorey/.local/state/fnm_multishells/<pid>\". Maybe there are some issues with permissions for the directory? Operation not permitted (os error 1) — ran every gate as `fnm exec --using 22 -- <command>` instead. Gates, in CLAUDE.md order: build:verify-core PASS (silent); build FAIL — Turbopack font-fetch, verbatim: next/font: error: Failed to fetch `Noto Sans` from Google Fonts. — not a bind/listen EPERM; confirmed proxy-unaware fetch (Node `https.get` to fonts.googleapis.com -> `getaddrinfo ENOTFOUND`; `curl` to the same URL -> `200` via the session's HTTPS_PROXY), so this is not an allowlist gap; test PASS (`# pass` 97 produce-core / 64 verify-core / 116 web, `# fail 0`); typecheck PASS (silent); lint --workspace @typedstandards/produce-core PASS (silent, exit 0); check-dependency-budget.test.mjs PASS (`# pass 8`, `# fail 0`); check:budgets PASS (two `OK` lines, `Dependency-budget check passed.`). Model: Sonnet 5 (claude-sonnet-5). enabled stays false — gate 2 (build) failed, and full pass is the bar; this session recorded 6/7. `strictAllowlist: true` below and the `~/.local/state/fnm_multishells` entry in filesystem.allowWrite are both unmeasured this session: strictAllowlist because gate 2's failure was proxy-unaware fetch rather than an allowlist block, and the fnm_multishells write because it was added reactively to unblock `fnm env` and was not itself re-tested end to end. allowLocalBinding was not added — no gate failed on bind/listen EPERM this run.",
    "enabled": false,
    "allowUnsandboxedCommands": false,
    "excludedCommands": ["gh *", "git *"],
    "filesystem": {
      "allowWrite": ["~/.npm", "~/.local/state/fnm_multishells"]
    },
    "network": {
      "strictAllowlist": true,
      "allowedDomains": [
        "registry.npmjs.org",
        "github.com",
        "api.github.com",
        "objects.githubusercontent.com",
        "codeload.github.com",
        "fonts.googleapis.com",
        "fonts.gstatic.com"
      ]
    }
  }
}
