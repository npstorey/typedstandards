{
  "name": "fixture-unbudgeted-pkg",
  "version": "0.0.0",
  "private": true,
  "description": "Budget-check fixture (failure mode b): the manifest declares a dependency (fake module `left-pad`) that is not in the budget allowlist. Never installed or built.",
  "dependencies": {
    "left-pad": "^1.3.0"
  }
}
